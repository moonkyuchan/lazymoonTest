import React from "react";
import styled from "styled-components";

const Visitor = () => {
  return <VisitorBack>방명록</VisitorBack>;
};

const VisitorBack = styled.section`
  width: 50%;
  height: 700px;
  background-color: yellow;
`;

export default Visitor;
